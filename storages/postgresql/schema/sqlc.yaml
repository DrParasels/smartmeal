# Конфигурация для sqlc
version: "2"

sql:
  # Настройка для PostgreSQL
  - engine: "postgresql"
    # Путь к директории с SQL-запросами
    queries: "queries"
    # Путь к файлам миграций/схемы БД
    schema:
      - "migrations/*.up.sql"
    gen:
      go:
        # Название пакета сгенерированного кода
        package: "sqlc"
        # Выходная директория для сгенерированного кода
        out: "sqlc"
        sql_package: "pgx/v5"
